# 喜剧行业垂直 Agent —— 框架开发计划

基于 [comedy-agent-design.md](comedy-agent-design.md) 中的**方案二（一体化 Agent 框架）**，技术栈为 **LangChain + 多模型协作 + RAG 知识库 + 记忆系统**。

---

## 一、开发阶段总览

| 阶段 | 主题 | 预计周期 | 核心目标 |
|------|------|----------|----------|
| 第一阶段 | MVP 骨架搭建 | 2-3 周 | 跑通 Agent → Skill → LLM → Output 的最小闭环 |
| 第二阶段 | Skill 体系与模型层 | 3-4 周 | 构建完整喜剧 Skill 生态，支持模型动态切换与 Fallback |
| 第三阶段 | RAG 知识库建设 | 3-4 周 | 让 Agent 具备喜剧行业专业知识检索与注入能力 |
| 第四阶段 | 记忆系统与用户层 | 2-3 周 | 实现个性化记忆与持续进化 |
| 第五阶段 | 工程化与优化 | 持续迭代 | 性能、可观测性、生产就绪 |

---

## 二、各阶段详细任务

### 第一阶段：MVP 骨架搭建（2-3 周）

> **目标**：跑通最基础的 Agent 调用链路，验证技术选型可行性。

| 序号 | 任务 | 说明 |
|------|------|------|
| 1.1 | 项目脚手架搭建 | 初始化 Python 项目结构，配置依赖管理（LangChain、向量库、HTTP 框架等） |
| 1.2 | ModelFactory 实现 | 统一封装 OpenAI、Anthropic、Ollama、通义千问等接口，支持配置化模型切换 |
| 1.3 | 基础 Agent 主控 | 基于 LangChain Agent 实现最简 Orchestrator，能接收用户输入并路由到对应工具 |
| 1.4 | 首个 MVP Skill | 实现一个最小可用 Skill（如"脱口秀创作"），验证 Tool → Prompt → LLM → Output 链路 |
| 1.5 | 基础交互接口 | 提供命令行 CLI 或简单 HTTP API 供内部调试 |

**里程碑**：输入一个创作需求，Agent 能调用 Skill 返回一个基本可用的脱口秀段子。

---

### 第二阶段：Skill 体系与模型层完善（3-4 周）

> **目标**：构建完整的喜剧 Skill 生态，模型层稳定可用。

| 序号 | 任务 | 说明 |
|------|------|------|
| 2.1 | 全量 Skill 开发 | 实现相声、小品、情景喜剧、辅助工具（笑点分析、剧本评估等）等 Skill |
| 2.2 | Skill 插件化机制 | 支持从 `skills/` 目录动态加载（读取 `SKILL.md` + `prompt.txt` 自动注册） |
| 2.3 | 模型分层配置 | 按任务类型绑定模型：创意任务 → Claude 3.5 Sonnet / GPT-4o，分析任务 → GPT-4o / Qwen2.5，快速响应 → 轻量模型 |
| 2.4 | 模型自动 Fallback | 主模型失败（超时/限流/异常）时自动降级到备用模型（如 API 故障 → 本地 Qwen2.5-72B） |
| 2.5 | Prompt 模板工程化 | 建立统一的 Prompt 管理模块，支持变量注入、版本管理、A/B 测试 |

**里程碑**：Agent 能识别用户需求类型，自动选择对应喜剧 Skill 和最优模型完成创作。

---

### 第三阶段：RAG 知识库建设（3-4 周）

> **目标**：让 Agent 具备喜剧行业专业知识，实现"懂行"的创作辅助。

| 序号 | 任务 | 说明 |
|------|------|------|
| 3.1 | 文档加载流水线 | 实现 PDF / Word / 网页 / 字幕的解析入库（基于 Unstructured / DocumentLoader） |
| 3.2 | 智能分块策略 | 按"场景"、"笑点单元"、"角色对话"分块，保留类型、角色、场景等元数据 |
| 3.3 | 向量数据库搭建 | 接入 Embedding 模型（text-embedding-3-large / BGE-M3），部署 ChromaDB（本地）或 Milvus（生产） |
| 3.4 | 混合检索实现 | 向量检索（语义相似）+ BM25（关键词检索）+ Cross-Encoder 重排序 |
| 3.5 | 上下文注入机制 | 检索结果自动拼接进 System Prompt / User Context，控制 Token 预算 |
| 3.6 | 行业数据冷启动 | 导入首批核心数据：《喜剧的艺术》、郭德纲相声理论、春晚小品剧本、喜剧结构理论等 |
| 3.7 | 喜剧行业特殊优化 | 结构感知分块、多向量表示探索（内容向量 + 结构向量 + 风格向量） |

**里程碑**：用户询问"三番四抖怎么写"时，Agent 能基于知识库给出结合理论的定义和创作示例。

---

### 第四阶段：记忆系统与用户层（2-3 周）

> **目标**：实现个性化、持续进化的用户交互体验。

| 序号 | 任务 | 说明 |
|------|------|------|
| 4.1 | 记忆库 Schema 设计 | 区分短期记忆（会话级）和中期记忆（用户级），定义存储结构 |
| 4.2 | 记忆存取接口 | 实现用户偏好、历史对话、创作习惯的写入与读取 |
| 4.3 | 记忆与 RAG 融合 | 在生成阶段同时注入记忆上下文（关于用户）和知识上下文（关于喜剧） |
| 4.4 | 用户创作管理 | 支持保存、查看、编辑用户历史剧本，建立个人作品库 |
| 4.5 | 高评分内容回流 | 实现用户高评分剧本的自动解析、分块、增量入库，让知识库持续进化 |

**里程碑**：Agent 能记住"用户喜欢黑色幽默、讨厌谐音梗"，并在后续创作中自动调整风格。

---

### 第五阶段：工程化与优化（持续迭代）

> **目标**：系统性能、可观测性、扩展性达到生产标准。

| 序号 | 任务 | 说明 |
|------|------|------|
| 5.1 | 多向量表示优化 | 同一文档生成内容向量 + 结构向量 + 风格向量，提升检索精度 |
| 5.2 | 缓存与并发优化 | 高频查询缓存（Redis）、多用户并发支持、限流保护 |
| 5.3 | 可观测性建设 | 接入 LangSmith 或自建日志系统，追踪调用链、监控延迟与 Token 消耗 |
| 5.4 | 评估体系 | 建立剧本质量、检索相关性、模型输出的自动化评估指标与回归测试 |
| 5.5 | 部署与交付 | Docker 化、CI/CD 流水线、API 网关、Web 前端 Demo |

---

## 三、关键依赖路径

```
ModelFactory（第一阶段）
    ↓
Skill 体系（第二阶段） ──→ 基础创作能力可用
    ↓
RAG 知识库（第三阶段） ──→ 专业创作能力可用
    ↓
记忆系统（第四阶段） ──→ 个性化创作能力可用
    ↓
工程化优化（第五阶段） ──→ 生产就绪
```

---

## 四、技术选型确认

| 层级 | 技术选型 | 备注 |
|------|----------|------|
| Agent 框架 | LangChain | 生态成熟，Tool/Memory/Chain 抽象完善 |
| 模型接入 | ModelFactory 统一封装 | 支持 OpenAI、Anthropic、Ollama、通义千问等 |
| Embedding | text-embedding-3-large / BGE-M3 | 根据成本和效果选择 |
| 向量数据库 | ChromaDB（开发/小规模）→ Milvus（生产/大规模） | 按需切换 |
| 记忆存储 | PostgreSQL / SQLite（结构化）+ Redis（缓存） | 短期记忆可放 Redis，中期记忆持久化到数据库 |
| 文档解析 | Unstructured / LangChain DocumentLoader | 支持多格式 |
| 检索增强 | BM25 + Cross-Encoder | 混合检索 + 重排序 |
| 可观测性 | LangSmith / 自建日志 | 优先 LangSmith，成本敏感时自建 |

---

## 五、风险与应对

| 风险点 | 影响 | 应对策略 |
|--------|------|----------|
| 喜剧数据版权敏感 | 知识库冷启动受阻 | 优先使用公开理论资料，用户私有数据由用户自行导入 |
| 模型创意输出不稳定 | 剧本质量波动大 | 建立评估体系 + Prompt 版本管理 + 模型 Fallback |
| Token 成本过高 | RAG + 长剧本导致费用激增 | 上下文压缩、检索结果截断、本地模型兜底 |
| Skill 过多导致路由混乱 | Agent 调用错误 Skill | 增加意图分类层，明确 Skill 描述和边界 |

---

*计划制定基于 `comedy-agent-design.md` 方案二，可根据实际开发进度和资源情况动态调整优先级。*
