# 任务执行记录

## 任务信息
- **阶段**: 第 6 阶段 —— 专业版工作流
- **任务编号**: 6.6-fix5
- **任务名称**: 专家多轮交互与 advance 交棒信号
- **执行日期**: 2026-06-20

## 任务说明
用户希望每个核心专家不是一收到输入就填槽交接，而是能跟用户多轮讨论、给建议、引导完善，最终由专家主动判断素材足够后再交棒给下一个角色。

## 完成内容
- **引擎层**：`ProWorkflowEngine.process` 移除自动 cue 逻辑，只有在 skill 返回 `advance: true` 且 `next_role` 发生变化时才切换当前角色
- **Skill 层**：
  - 新增 `advance` 字段，普通填槽默认 `false`，支持当前角色多轮交互
  - `@mention` 或语义跳转等显式角色切换自动 `advance: true`
  - `_compute_next_state` 只在 `advance` 为 true 时推进状态机，否则保持当前状态
- **意图分类**：移除「当前角色可填槽且用户输入非提问就自动填槽」的规则，避免多轮讨论时把每句回答都当成槽位值
- **Prompt 层**：`_render_role_prompt` 为核心专家动态注入 `advance` 交互规则与 JSON 示例，覆盖 prompt 文件中的旧示例
- 运行相关测试：`test_pro_workflow_skill_mapping.py`、`test_admin_workflow.py`，全部通过

## Commit 记录
- **Commit ID**: `fcefaf8d206024bdf0c361368f91d660661eb5ae`
- **Commit Message**: `task 6.6-fix5: 专家多轮交互与 advance 交棒信号`
- **Branch**: `v3`
- **Remote**: `origin/v3`

## 备注
- 测试通过率: 20/20 (100%)
- `data/prompts/pro/*.md` 默认被 `.gitignore` 忽略，已同步修改本地 prompt 文件；代码中已注入相同的 `advance` 规则作为兜底，确保部署后行为一致

## 做完 A 后的下一步计划建议
1. **方案 B：skill 完全接管状态机**
   - 删除/简化 `data/pro_workflow.json`，引擎不再根据 workflow 配置决定 `action`
   - skill 直接根据 `current_state` 判断是 collect、ask、generate 还是 done
   - admin 工作流接口只保存轻量配置（如初始状态、角色列表）
2. **Prompt 版本化管理**
   - 如果希望 prompt 文件也进入版本控制，需要调整 `.gitignore`，把 `data/prompts/pro/` 从忽略列表中释放出来
3. **专家回复质量调优**
   - 跑真实对话，观察专家是否还急于交接、是否会在多轮中重复提问，再针对性调整 prompt 或增加「用户确认检测」规则
