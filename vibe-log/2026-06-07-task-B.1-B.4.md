# 任务执行记录

## 任务信息
- **阶段**: 第 B 阶段 —— 业务功能页面
- **任务编号**: B.1 ~ B.4
- **任务名称**: 演员工作台、管理控制台与现有页面改造
- **执行日期**: 2026-06-07

## 任务说明
基于 `plan-restructure.md` 阶段 3，创建演员工作台和管理控制台页面，并改造现有 scripts.html 和 me.html，使新业务功能可被用户访问。

## 完成内容

### B.1 演员工作台（actor-dashboard.html）
- 统计卡片：风格调用次数、预计收益、待审核投稿（GET `/actor/dashboard`）
- 投稿审核表格：展示投稿列表，支持采纳/拒绝操作（GET `/submissions` + POST `/submissions/{id}/review`）
- 收益明细表格：展示收益记录（GET `/actor/earnings`）
- 提现表单：输入金额提交提现申请（POST `/actor/withdraw`）

### B.2 管理控制台（admin-console.html）
- 平台概览统计卡片：DAU、总生成次数、IP 调用、加点盐使用、待结算分成（GET `/admin/overview`）
- Tab 切换：Skill 审核 / IP 模型管理 / 敏感词
- Skill 审核：待审核列表，支持通过/拒绝（GET `/admin/skills/pending` + POST `/admin/skills/{name}/review`）
- IP 模型管理：列表展示，支持编辑分成比例（GET `/admin/ip-styles` + PUT `/admin/ip-styles/{id}`）
- 敏感词管理：标签式展示，支持添加/删除（GET/POST/DELETE `/admin/banned-words`）

### B.3 改造 scripts.html
- 每个作品项增加「导出」按钮（GET `/scripts/{id}/export?format=txt|md`）
- 增加「投稿」按钮，弹出输入演员名称的对话框（POST `/scripts/{id}/submit`）

### B.4 改造 me.html
- 新增「演员工作台」入口卡片
- 新增「管理控制台」入口卡片

## Commit 记录
- **Commit ID**: `2ea9f846042dabfb1e5265d46586982ad2064747`
- **Commit Message**: `task B.1-B.4: 业务功能页面（演员工作台 + 管理控制台 + 作品页改造）`
- **Branch**: `refactor`
- **Remote**: `origin/refactor`

## 备注
- 新增 2 个前端页面，改造 2 个现有页面
- 全部页面复用 `common.css` / `common.js`
- 管理控制台和演员工作台后端已有 403 权限校验，前端无需额外处理
