# 专业版页面重构计划

## 目标
将 `frontend/pro.html` 按照 `prototype_get_style.html` 的视觉风格和交互范式进行全面重构，同时保留并映射现有专业版的所有业务功能。

## 概念映射（原型 → 本系统）

| 原型概念 | 本系统对应 |
|---------|-----------|
| 意辰 | 当前登录用户（创作主理人） |
| Get达人 | 系统内置人格化技能助手（资料与结构助手） |
| 写作团队成员 | 话题、态度、情绪、风格等 Skill 的拟人化 |
| 脱口秀写法探讨 | 一个项目/对话 |
| 新建对话 | 新建项目/对话 |
| 对话历史 | 查看历史项目，加载以前数据 |
| 分享 | 预留接口，暂不实现 |
| 查看文件 | 预留接口，暂不实现 |
| @按钮 | @话题/态度/情绪/风格等拟人化 Skill |
| 左侧面板 | 与 Skill 的对话区域 |
| 右侧面板 | 最终剧本输出结果 |
| 写作进程 | 生成进度/步骤追踪 |

---

## 重构步骤

### Phase 1：基础布局重构

**1.1 整体布局改造**
- 将现有的 `sidebar + card 网格` 布局替换为原型的 `topbar + 左右分栏` 布局
- 全屏高度 (`100vh`)，无滚动条（大屏），响应式适配移动端
- 移除现有的蓝色 header，改为原型的白色毛玻璃 topbar

**1.2 Topbar 实现**
- 左侧：**写作团队**按钮
  - 显示 3 个层叠头像（当前用户、Get达人、团队成员代表）
  - 显示成员数量（如"（9）"）
  - 点击展开下拉菜单，展示：意辰（创作主理人）、Get达人（资料与结构助手）、写作团队成员（共 N 人在线）
- 中间：当前**项目名称**（如"脱口秀写法探讨"），可显示"正在生成…"状态
- 右侧：操作按钮组
  - **新建对话**（`+` 图标）：新建项目/对话
  - **对话历史**（时钟图标）：打开历史面板
  - **分享**（分享图标）：预留接口，点击打开分享弹窗（仅展示）
  - **查看文件**（文件夹图标）：预留接口，点击打开文件面板（仅展示）

### Phase 2：左侧面板重构（Skill 对话区）

**2.1 对话内容区**
- 显示与各个 Skill 的对话流（替代原有的 card 布局）
- 每个 Skill 的回复以卡片/段落形式展示
- 保留原有的大纲输入逻辑，但以对话形式呈现

**2.2 写作进程面板**
- 可展开/收起的进度追踪面板
- 显示当前生成步骤（如"2/7"）
- 任务状态：已完成（✓）、进行中（动画）、待处理（空心）
- 点击任务可切换/跳转

**2.3 底部输入区**
- 左下角 **@** 按钮：点击自动填充 `@Get达人 ` 到输入框，聚焦输入框
- 输入框：contenteditable，支持占位符提示
- 左侧图标：附件/添加、语音、引用的文档 pill
- 右侧图标：格式、地球、菜单、发送按钮
- 输入框聚焦时有高亮动画效果
- 按 Enter 发送消息

### Phase 3：右侧面板重构（结果展示区）

**3.1 最终结果展示**
- 以编辑器形式展示生成的剧本/内容
- 支持段落级悬停动画效果
- 生成时有打字机效果和"正在生成…"状态
- 新内容有淡入动画

**3.2 窗口工具栏**
- 右上方浮动工具栏：复制、保存、下载、关闭
- 每个按钮有 hover 效果和点击反馈

**3.3 历史记录入口**
- "查看历史记录"按钮，可快速打开历史面板

### Phase 4：弹层与面板

**4.1 团队下拉菜单**
- 展示当前用户（意辰）、Get达人、写作团队成员列表
- 每个成员有头像和角色描述

**4.2 对话历史面板**
- 右侧滑出面板
- 展示历史项目列表
- 每个项目显示名称、更新时间、状态
- 点击可加载历史项目数据

**4.3 分享弹窗（预留）**
- 居中模态框
- 显示分享链接占位
- 复制按钮（仅展示反馈）

**4.4 查看文件面板（预留）**
- 右侧滑出面板
- 展示文件列表占位

**4.5 遮罩层**
- 统一的半透明遮罩，点击关闭所有面板/弹窗

### Phase 5：数据与交互映射

**5.1 Skill 拟人化映射**
- 加载 `/pro/skills` 接口获取的话题、态度、情绪、风格 Skill
- 将每个 Skill 映射为"写作团队成员"
- Get达人作为固定内置成员
- 当前登录用户作为"意辰"

**5.2 对话逻辑**
- @某个 Skill 时，左侧显示与该 Skill 的对话
- 对话发送后，保留原有 API 调用逻辑（`/pro/generate`）
- 生成结果展示在右侧

**5.3 项目管理**
- 新建对话：清空当前状态和输出，创建新项目
- 对话历史：维护本地项目历史列表，支持加载
- 当前项目名称可自定义（默认"新对话"或按大纲自动生成）

**5.4 保留原有功能**
- 人物画像（Rules）选择 → 可在对话中通过 Get达人 引导或作为团队配置
- 模型选择 → 保留在输入区或设置中
- Token 预估 → 保留在生成前的提示中
- 新建画像 Modal → 保留，通过团队菜单或配置入口打开

### Phase 6：样式与动效

**6.1 CSS 变量体系**
- 沿用原型的颜色变量：`--bg`, `--ink`, `--muted`, `--light`, `--line`, `--orange`, `--green`, `--icon`, `--shadow`

**6.2 动效实现**
- 头像 hover 展开效果
- 按钮点击涟漪效果 (ripple)
- 面板/弹窗的展开/收起动画
- 打字机效果（生成内容时）
- 段落悬停位移动画
- Toast 轻提示

**6.3 响应式适配**
- 大屏：左右分栏
- 小于 1100px：垂直堆叠，sticky 底部输入框

---

## 文件变更

- **修改**：`frontend/pro.html` —— 全面重构 HTML、CSS、JS
- **无需修改**：后端 API 接口保持不变

## 验证要点

1. 页面加载后正常显示 topbar + 左右分栏布局
2. 写作团队下拉菜单可正常展开/收起
3. Skill 列表正确加载并映射为团队成员
4. @按钮可将 @Get达人 填入输入框
5. 输入内容后按 Enter 可发送（保留原有 API 调用）
6. 生成结果正确展示在右侧面板
7. 对话历史面板可正常打开，显示历史记录
8. 新建对话可清空状态并创建新项目
9. 写作进程面板可展开/收起
10. 响应式布局在移动端正常显示
