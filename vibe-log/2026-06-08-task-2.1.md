# 任务执行记录

## 任务信息
- **阶段**: 第 2 阶段 —— API 层重构
- **任务编号**: 2.1
- **任务名称**: 重构极速版 API（/speed）
- **执行日期**: 2026-06-08

## 任务说明
将现有的"加点盐"功能升级为极速版趣味加工引擎，支持 IP 角色选择和 Token 预估。

## 完成内容
- 新增 `src/comedy_agent/api/routers/speed.py`：
  - `POST /speed/polish`: 趣味加工核心接口
    - 请求：`text`, `intensity`, `ip_role_id`(可选), `project_id`(可选), `model`(可选)
    - 响应：`original`, `polished`, `ip_role`(头像/名称/主页链接), `token_cost`, `estimated_tokens`
    - 若传入 `ip_role_id`，加载对应 IPRole 的 `prompt_snippet` 注入 prompt
    - 调用 orchestrator → add_salt skill 执行润色
  - `GET /speed/ip-roles`: 列出 status=active 的 IP 角色
  - `GET /speed/history`: 过滤 source="speed" 的生成历史
  - `POST /speed/estimate`: 按字符数估算 Token（300 字符 ≈ 240 tokens）
- `api/server.py`: 注册 `speed_router`
- 保留现有 `/salt` 端点向后兼容

## Commit 记录
- **Commit ID**: `0f988e7f72bad6cad9b833c75bafee7ef95b6f1c`
- **Commit Message**: `task 2.1: 重构极速版 API（/speed）`
- **Branch**: `v2`
- **Remote**: `origin/v2`

## 备注
- 极速版 router 语法验证通过
- 模型调用仍走 orchestrator 统一接口，符合重构规则
