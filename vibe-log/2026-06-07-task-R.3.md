# 任务执行记录

## 任务信息
- **阶段**: 第 R 阶段 —— 重构与扩展
- **任务编号**: R.3
- **任务名称**: 拆分 API Router 并接入 Token 扣费
- **执行日期**: 2026-06-07

## 任务说明
将 monolithic 的 `server.py` 拆分为多个子 Router 文件，并实现新功能接口（钱包、项目、加点盐、IP 风格、投稿、演员、管理后台、导出），同时将 Token 扣费机制接入现有的聊天和 Skill 调用端点。

## 完成内容
- 新增 8 个 APIRouter 子文件：
  - `wallet.py`: `/me/wallet`, `/me/recharge`
  - `projects.py`: `/projects/*` CRUD
  - `salt.py`: `/salt`（加点盐润色）
  - `ip_styles.py`: `/ip-styles/*`
  - `submissions.py`: `/submissions/*`, `/scripts/{id}/submit`
  - `actor.py`: `/actor/*`（演员后台）
  - `admin.py`: `/admin/*`（平台管理、敏感词）
  - `export.py`: `/scripts/{id}/export`
- 提取共享 `AppState` 到 `comedy_agent.api.state`，消除循环依赖
- `server.py` 引入所有子路由并挂载，文件从 1408+ 行精简
- `/chat` 与 `/skills/*`（standup/sketch/manzai/japanese-sketch）接入 Token 余额检查与扣费：
  - 余额不足返回 HTTP 402
  - 扣费在成功响应后执行
- 新增 `tests/test_api_new_routers.py`，覆盖钱包、项目、加点盐、IP 风格、管理后台共 10 个 API 测试用例

## Commit 记录
- **Commit ID**: `bcd1a16dcf1ff1eb12279a79dbb518f722805dc8`
- **Commit Message**: `task R.3: 拆分 API Router 并接入 Token 扣费`
- **Branch**: `refactor`
- **Remote**: `origin/refactor`

## 备注
- 测试通过率: 28/28（新测试全部通过）
- `test_preference_extractor.py` 存在预存在导入错误（非本任务引入）
- `test_memory_medium_term.py::TestPreferences::test_save_and_load_preference` 为已知预存失败（preference store 为 stub）
