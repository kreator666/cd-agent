# 用户中期记忆系统 —— 后续开发计划

> 本计划基于当前已实现的中期记忆系统（UserProfile / UserPreference / UserConversation / UserScript），梳理剩余缺口与优化项。
>
> 当前后端闭环已完整，前端作品管理为最大缺口。

---

## 一、P0 —— 阻塞/核心功能缺口

### 1. 前端「我的作品」面板

**现状**：后端 `/scripts` 全套 CRUD + `/scripts/{id}/rate` + `/feedback/ingest` 已就绪，但 `frontend/index.html` 完全没有作品相关 UI。

**影响**：用户无法感知作品库能力，无法查看、保存、编辑、评分自己的剧本。第四阶段 4.4「用户创作管理」后端 100% 但前端 0%。

**需求**：
- 在侧边栏新增「我的作品」折叠面板（与「历史会话」「我的偏好」并列）
- 列表展示：标题、类型、评分、更新时间
- 点击展开查看完整内容
- 支持删除作品
- 支持修改评分（星级/滑块）
- 可选：简单的编辑标题/内容功能

**关联后端**：`GET /scripts`、`GET /scripts/{id}`、`DELETE /scripts/{id}`、`PATCH /scripts/{id}/rate`

**工作量**：中

---

### 2. 移除调试代码残留

**位置**：`src/comedy_agent/agent/orchestrator.py` 第 131 行
```python
print("----------chat_history--------------",chat_history)
```

**影响**：污染生产日志，每次对话都会输出无意义的调试信息。

**修复**：直接删除该行。

**工作量**：极小

---

## 二、P1 —— 体验优化

### 3. 偏好提取增加触发门槛

**现状**：每次 `/chat` 请求结束后都调用 `extract_preferences()`，哪怕只有 1 轮对话（甚至用户只打了个招呼）也触发，浪费 fast 模型 Token。

**建议阈值**（满足任一即触发）：
- 消息总数 ≥ 3 条
- 对话总字数 > 200 字
- 或两者同时满足

**修改位置**：`src/comedy_agent/api/server.py` `/chat` 接口中调用 `extract_preferences` 之前加判断。

**工作量**：小

---

### 4. 优化偏好提取 Prompt

**现状**：`DEFAULT_PROMPT` 无 Few-shot 示例，单条消息硬截断到 300 字符（`_build_conversation_text` 中的 `[:300]`），可能丢失关键偏好信号。

**优化方向**：
- 加入 1-2 个 Few-shot 示例，展示如何从对话中准确提取偏好
- 放宽单条消息截断限制（如 1000 字符），或改为按总 Token 数截断
- 增加字段解释：说明各字段的业务含义和取值示例
- 增加「无变化则输出 `{}`」的指令，避免无意义提取

**修改位置**：`src/comedy_agent/memory/preference_extractor.py`

**工作量**：小

---

### 5. 聊天界面「保存为作品」按钮

**现状**：用户看到 Agent 生成的好内容后，没有办法一键保存到个人作品库，只能走 API/CLI。

**需求**：
- 每条 AI 消息下方增加「💾 保存为作品」按钮
- 点击后弹出小窗：输入标题、选择类型（standup/sketch/crosstalk/sitcom）
- 调用 `POST /scripts` 保存
- 保存成功后提示，并自动刷新「我的作品」面板

**工作量**：中

---

### 6. 聊天界面「评分」交互

**现状**：作品评分能力只在后端 API 和 CLI 中可用，前端无评分 UI。

**需求**：
- 在「我的作品」面板的作品详情中增加星级评分（1-5 星）
- 调用 `PATCH /scripts/{id}/rate` 更新评分
- 评分后视觉反馈（高亮星级）

**工作量**：小

---

## 三、P2 —— 工程优化

### 7. 优化 `build_context_text` 截断策略

**现状**：超 Token 预算时直接丢弃整段低优先级内容（如整段「近期作品」），而不是在段落内部智能截断。可能导致一条作品都没有注入，信息丢失严重。

**优化方向**：
- 对「近期作品」段落内部按单条作品截断，保留前 N 条（已在 `max_scripts` 中部分实现）
- 对「近期会话摘要」段落内部按会话截断
- 对超长单条内容（如单条作品 5000 字）做文本截断，保留前 200 字 + "..."

**修改位置**：`src/comedy_agent/memory/unified.py` `build_context_text()`

**工作量**：小

---

### 8. 优化 `FeedbackLoop._fetch_scripts` 查询性能

**现状**：传入 `user_id` 时，先 `list_scripts(user_id)` 在 SQL 层加载该用户**全部**作品，再在 Python 层按 `min_rating` 过滤。用户作品多时不高效。

**优化方向**：
- `SQLMemoryStore.list_scripts()` 增加 `min_rating` 参数，直接在 SQL 层 `WHERE rating >= ?` 过滤
- `FeedbackLoop._fetch_scripts()` 透传该参数

**工作量**：小

---

### 9. 偏好提取增加置信度/时间戳（P3）

**现状**：偏好只有 key-value，无提取时间、无置信度。陈旧偏好可能长期覆盖新偏好。

**优化方向**：
- `UserPreference` 表已有 `updated_at`，但未在展示层使用
- 可在偏好面板上显示「X 天前更新」
- 高阶：为偏好值附加置信度分数，低置信度偏好不注入上下文

**工作量**：小（时间戳展示）/ 中（置信度系统）

---

## 四、对照 plan.md 第四阶段目标

| 任务 | 目标 | 当前完成度 | 缺口 |
|------|------|-----------|------|
| 4.1 记忆库 Schema 设计 | 区分短期/中期记忆，定义存储结构 | ✅ 100% | 无 |
| 4.2 记忆存取接口 | 偏好、历史对话、创作习惯的读写 | ✅ 100% | 无 |
| 4.3 记忆与 RAG 融合 | 生成阶段同时注入记忆上下文 + 知识上下文 | ✅ 100% | 无 |
| **4.4 用户创作管理** | 保存、查看、编辑历史剧本，建立个人作品库 | ⚠️ **后端 100%，前端 0%** | **P0 项 1 + P1 项 5/6** |
| 4.5 高评分内容回流 | 高评分剧本自动解析、分块、增量入库 | ✅ 100% | 无 |

---

## 五、建议执行顺序

```
1. 移除调试代码（P0-2，1分钟）
2. 前端「我的作品」面板（P0-1，核心功能）
3. 偏好提取触发门槛（P1-3，快速优化）
4. 聊天界面「保存为作品」按钮（P1-5，与 P0-1 联动）
5. 聊天界面「评分」交互（P1-6）
6. 优化偏好提取 Prompt（P1-4）
7. build_context_text 截断策略优化（P2-7）
8. FeedbackLoop 查询性能优化（P2-8）
```

---

*计划更新日期：2026-05-21*
