# 任务执行记录

## 任务信息
- **阶段**: 第 D 阶段 —— 专业版提示词增强
- **任务编号**: D.4
- **任务名称**: 新增 standup_v2 教练 Skill 与教练模式写作流程
- **执行日期**: 2026-06-30

## 任务说明
用户要求：
1. 把 `skills/standup/SKILL.md` 的参数表（加入 4 维度槽位）与 `skills/standup_coach/system_prompt.md` 融合，生成新 Skill `standup_v2`。
2. 将 `WriterAgent` 从“每次生成正文”改为“写稿教练”：输出写作提示，由用户根据提示自行撰写段落。

## 完成内容
- 新增 `skills/standup_v2/SKILL.md`：
  - 旧版 SKILL.md 格式，含 YAML frontmatter、`metadata.mode: coach`、`metadata.kind: standup`。
  - 参数表包含 topic/attitude/bias/emotion（对应 4 维度槽位）以及 style/duration/audience/density/perspective_count/debug。
  - 系统提示词融合 standup_coach 的 BVT、ER、观众五阶段、四阶段输入、加梗技法库、质量清单与 standup 的五感幽默系统、开放麦写法原则。
  - 提示词模板为教练专用：要求只输出写作提示，不写正文。
- 改造 `src/comedy_agent/agents/writer.py`：
  - 加载 Skill 后检测 `metadata.mode == "coach"`。
  - Coach 模式：LLM 输出作为 `coaching_hints`，返回 `phase: "drafting"`，不修改 `sections`。
  - 自动模式：保持原有写作行为。
- 新增 `src/comedy_agent/nodes/draft_node.py`：
  - 使用 `interrupt()` 展示教练提示并等待用户草稿。
  - 恢复后将用户输入写入 `sections[current_section]`，进入 `reviewing`。
- 状态扩展 `src/comedy_agent/state/schema.py`：
  - 新增 `coaching_hints` 字段。
  - `phase` 枚举增加 `"drafting"`。
- 图路由扩展：
  - `src/comedy_agent/agents/supervisor.py`：增加 `"drafting" -> "draft_node"` 路由。
  - `src/comedy_agent/graph/supervisor_graph.py`：注册 `draft_node`。
  - `src/comedy_agent/nodes/__init__.py`：导出 `draft_node`。
- API 适配 `src/comedy_agent/api/routers/pro_v4.py`：
  - `_extract_interrupt_info()` 识别 `coaching_hints` 为 `review_type: "drafting"`。
  - `_build_response()` 构造 `workflow_state="drafting"` 响应，返回教练提示。
  - `pro_chat_v4()` 把 `drafting` 阶段视为 resume，用户输入作为草稿。
- 前端适配 `frontend/pro-b.html`：
  - `renderGuideContentHtml` 对 `workflowState === 'drafting'` 渲染教练提示卡。
  - 增加 `setInputPlaceholder()`，根据工作流状态切换输入框 placeholder。
  - “提交草稿”按钮仅聚焦输入框，用户输入后发送。
- 测试：
  - 新增 `tests/test_standup_v2_skill.py`：验证 Skill 加载、coach 模式、融合提示词、模板变量。
  - 新增 `tests/test_draft_node.py`：验证草稿收集与写入。
  - 更新 `tests/test_writer.py`：新增教练模式返回 `drafting` 断言。
  - 更新 `tests/test_supervisor.py`：增加 `drafting` 路由断言。

## Commit 记录
- **Commit ID**: `f295afaee3e17ba1ed1e32c486a4a83f5b742740`
- **Commit Message**: `task D.4: 新增 standup_v2 教练 Skill 与教练模式写作流程`
- **Branch**: `v3_new`
- **Remote**: `origin/v3_new`

## 备注
- 相关回归测试通过：96 项通过（含 `test_standup_v2_skill`、`test_draft_node`、`test_writer`、`test_supervisor`、`test_skill_router`、`test_knowledge_e2e`、`test_pro_v4_knowledge`、`test_writer_knowledge`、`test_skill_standup_coach`、`test_guide_agent`、`test_pro_v4`、`test_state_machine`、`test_interrupt`、`test_skills_loader`）。
- 参考文档 `docs/design/v4/脱口秀文稿创作Agent_系统提示词.md` 保持未跟踪状态，未纳入本次 commit。
