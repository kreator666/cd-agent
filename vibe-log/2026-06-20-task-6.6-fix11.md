# 任务执行记录

## 任务信息
- **阶段**: 第 6 阶段 —— 专业版工作流 Pro Workflow
- **任务编号**: 6.6-fix11
- **任务名称**: 修复按小节生成与 TODO 进行中状态显示
- **执行日期**: 2026-06-20

## 任务说明
用户反馈两个遗留问题：
1. 选择“按小节生成”后，系统仍然一次性输出完整剧本，而不是按小节逐段生成。
2. TODO 看板/创作流程列表没有显示“进行中”状态（虽然 fix6 声称已实现）。

本次修复针对这两个问题做最小改动并回归验证。

## 完成内容
- **修复按小节生成逻辑**
  - 在 `skills/get_daren/skill.py` 的 `_generate_script_content` 中，当传入 `section` 参数时，直接调用 LLM 生成当前小节正文，不再经过 `standup_generator` 技能。
  - 新增专门的系统提示与用户提示，要求模型“严格只输出当前小节的剧本正文，不要输出其他小节，不要输出完整剧本，不要输出小节标题”。
  - 单节字数限制在 300-600 字之间，保持输出可控。
- **补齐前端进行中状态渲染**
  - 更新 `frontend/pro.html` 的 `renderChecklist`：未完成的 `in_progress` 项显示 🟡 图标、橙色文字与左侧橙色高亮边框。
  - 更新 `renderTodoBoard`：`in_progress` 项添加 `in-progress` CSS 类，复选框显示 `●`，并使用橙色边框/文字样式。
  - 新增 `.todo-item.in-progress` 与 `.todo-check.in-progress` 样式规则。
- **回归测试**
  - 运行 `tests/test_pro_workflow_skill_mapping.py` 与 `tests/test_admin_workflow.py`，20 个测试全部通过。

## Commit 记录
- **Commit ID**: `ab3e722ea7977170dceddba84f0be5c3d360b92a`
- **Commit Message**: `task 6.6-fix11: 修复按小节生成与 TODO 进行中状态显示`
- **Branch**: `v3`
- **Remote**: `origin/v3`（已推送）

## 备注
- 测试通过率: 20/20 (100%)
- 已推送至 `origin/v3`。
