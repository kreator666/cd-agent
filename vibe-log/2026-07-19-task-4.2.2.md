# 任务执行记录

## 任务信息
- **阶段**: 第 4 阶段 —— 一键发布与多平台分发
- **任务编号**: 4.2.2
- **任务名称**: 修复扫码登录 404 与发布 async 解包异常
- **执行日期**: 2026-07-19

## 任务说明
用户反馈一键发布页面存在两个问题：
1. 点击“扫码登录”时，请求 `POST /publish/bilibili/login-qrcode` 返回 `404 Not Found`。
2. B站登录成功并上传视频后，点击发布按钮报错 `"发布异常: cannot unpack non-iterable coroutine object"`。

## 完成内容
- 停止项目所有 Python 进程（PID 29352、22840），消除旧版本服务占用。
- 修复 `src/comedy_agent/publisher/publisher.py` 中 `MultiPlatformPublisher.publish_to_platform` 对 `adapter.pre_publish_check(content)` 未加 `await` 导致的元组解包错误。
- 修复 `MultiPlatformPublisher.cleanup_all` 中 `adapter.cleanup()` 未加 `await` 的隐患。
- 检查 `BilibiliAdapter.upload_video_entry` 与 `print_video_list_info`，确认其为同步方法，无需添加 `await`。
- 更新 `tests/test_publisher_api.py`：将 `pre_publish_check` 与 `cleanup` 的 mock 改为 `AsyncMock`，匹配新的异步调用。
- 重新启动后端服务：`python -m uvicorn comedy_agent.api.server:app --host 0.0.0.0 --port 8000 --reload`。
- 验证服务已就绪：`/health` 返回 200，`/publish/bilibili/login-qrcode` 返回 401（未携带 token，说明路由已正确注册，不再 404）。

## Commit 记录
- **Commit ID**: `cbf78c63ea5d235241ce152a646ba8ba237ca70c`
- **Commit Message**: `task 4.2.2: 修复发布流程 async 解包与扫码登录 404`
- **Branch**: `v3_new`
- **Remote**: `origin/v3_new`

## 备注
- 测试通过率：
  - `tests/test_publisher_api.py`：14/14 通过
  - 全量测试 `tests/`：80 通过，1 失败（`tests/test_auth.py::TestPreferences::test_list_preferences_empty`，返回 404，与本次修改无关，系已有的 `/preferences` 路由缺失问题）
- 服务已运行在 `http://localhost:8000`
